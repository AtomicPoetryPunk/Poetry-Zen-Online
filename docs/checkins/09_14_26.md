# nodemon vs vite
- nodemon vs vite:
    - vite: static site
    - nodemon: watch server process, rebuild sass etc
    - so: nodemon

# poetry zen

- poem tree is every draft
    - so moving a poem into a tree means merging the draft histories of two poems
    - DELETING A TREE IS A BIG DEAL. deleting a draft is a smaller deal. *make this obvious in the ui*
- epigraph is "for..." while "quotation" is formatted differently but is also important to distinguish from the work
    - what other "not the poem actually" is important to note?
    - ...postgraph? midquotes? acknowledgements?
    - "annotation" system? "type" "format" "position"?
- "context" is not necessary; it is ultimately better encapsulated within "notes", "version", "feedback", etc
    - formatting "feedback" so it exports with google doc comments...?
- "collection" vs "series"?
    - a series is like "The Order of Things" that isn't strictly for publication (like a collection) but is a collection of ordered poems. how are they different, as datatypes?

- mobile apps, offline apps, and hosting a database
    - offline app first

# development plan

1. offline
    - workflow
2. online
    - authentication
    - storage
    - running a whole gosh darn server
3. mobile
    - reflecting over with lynx
